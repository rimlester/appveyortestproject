before_build:
  - nuget restore

after_build:
  -  cmd: cd c:\inetpub\wwwroot
  -  cmd: dir
  -  cmd: msbuild TestingDeploy.xml /p:Configuration=Deploy;SolutionName=AppVeyor;ProjectName=TestWeb/TestWeb
